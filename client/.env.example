# Copy to .env and fill in if you need something other than the default.
# Never commit the real .env file.

# Base URL of the backend HTTP API. Defaults to http://localhost:8080 (the
# docker-compose dev backend) when unset.
VITE_API_BASE_URL=
